pull_request_rules:
  - name: automatic merge when CI passes and 2 reviews
    conditions:
      - author=dependabot-preview[bot]
      - "#approved-reviews-by=fannheyward"
      - base=master
    actions:
      merge:
        method: merge
